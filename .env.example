# Same-origin production deploys use /api. Leave unset to use the built-in /api default.
VITE_BACKEND_URL=/api

# Optional: URL of the hero product demo video (mp4/webm). When unset the
# editorial poster ("The new builders.") renders instead.
# VITE_HERO_VIDEO_URL=
